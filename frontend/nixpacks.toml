[variables]
NODE_ENV = "production"
STATIC_ROOT = "dist"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/write-runtime-env.mjs && node scripts/serve-static.mjs"
